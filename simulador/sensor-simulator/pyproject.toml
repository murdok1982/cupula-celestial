[project]
name = "sensor-simulator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.8",
    "structlog>=24.4",
    "numpy>=2.0",
    "typer>=0.12",
]
